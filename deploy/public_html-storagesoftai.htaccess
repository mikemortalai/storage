# Place this file as: public_html/storagesoftai/.htaccess
# (i.e. the Laravel project root that lives at /storagesoftai on the site)

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
